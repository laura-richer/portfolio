---
import Button from '@/atoms/Button.astro';
import heroImage from '@/assets/hero.png';
---

<div class="hero">
  <div class="hero__container container">
    <h1 class="h2 hero__title">
      I design and build beautiful responsive,<br/>
      eCommerce and CMS driven websites.
    </h1>
    <h2 class="h3 hero__subtitle">
      I am a Frontend developer based in Leeds.
    </h2>
    <Button text="See more" link="#projects" />
  </div>
  <img src={heroImage.src} class="hero__image" alt="I design and build beautiful responsive, eCommerce and CMS driven websites."></img>
</div>

<style lang="scss">
.hero {
  position: relative;
  height: 80vh;
  text-align: center;
  color: var(--color-white);
  overflow: hidden;

  &__image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    animation: heroAnimation 120s linear;
  }

  &__container {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    padding-top: var(--header-height);
    padding-bottom: 0;
    gap: var(--spacer-m);
  }
}

@keyframes heroAnimation {
  100% {
    transform: rotate(10deg) scale(1.5);
  }
}
</style>
